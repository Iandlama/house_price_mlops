services:
  api:
    build: ./api
    ports:
      - "8000:8000"
    volumes:
      - ../../models:/models:ro
    restart: unless-stopped

  app:
    build: ./app
    ports:
      - "8501:8501"
    environment:
      - API_URL=http://api:8000
    depends_on:
      - api
    restart: unless-stopped